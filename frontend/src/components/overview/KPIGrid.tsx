import { BatchSummary } from '../../types/api'
export function KPIGrid({ s }: { s: BatchSummary }) { return <div className="kpis"><K label="Components screened" value={s.total_components} meta="persisted screening state" /><K label="Safe" value={s.safe} meta="current disposition" cls="safe" /><K label="Review" value={s.review} meta="engineering attention" cls="review" /><K label="Reject" value={s.reject} meta="hard disposition" cls="reject" /><K label="Unknown" value={s.unknown} meta="insufficient evidence" /></div> }
function K({ label, value, meta, cls='' }: { label:string; value:number; meta:string; cls?:string }) { return <div className={`kpi ${cls}`}><div className="label">{label}</div><div className="value mono">{value}</div><div className="meta">{meta}</div></div> }
