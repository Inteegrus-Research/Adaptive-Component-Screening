import { ProgressivePoint } from '../../types/api'
import { num } from '../../utils/format'
export function ProgressiveTimeline({ points }: { points: ProgressivePoint[] }) { if (!points.length) return <div className="empty">Progressive burn-in history is not present in the active report directory.</div>; return <div className="progressTimeline"><div className="timelineRail">{points.map((p, i) => { const left = points.length === 1 ? 50 : i / (points.length - 1) * 100; return <div className="timelineNode" style={{ left: `${left}%` }} key={`${p.origin_h}-${i}`}><div className={`timelineDot ${String(p.decision).toLowerCase()}`} /><div className="timelineLabel">{num(p.origin_h, 0)}h</div><div className="timelineDecision">{p.decision}</div>{p.risk_score != null && <div className="timelineRisk">{num(p.risk_score, 2)}</div>}</div> })}</div></div> }
