# Runtime lock for the current Python 3.11 environment.
# ML artifacts MUST be retrained after installing this lock.
numpy==2.3.5
pandas==2.2.3
scipy==1.17.0
scikit-learn==1.9.0
joblib==1.5.3
PyYAML==6.0.3
fastapi==0.128.2
uvicorn==0.48.0
python-multipart==0.0.29
pytest==9.0.2
httpx
# Optional but recommended for tree-model attributions. The evidence-based explainer remains available if SHAP is unavailable.
shap==0.50.0
# Optional model-attribution dependency (not required for core screening):
# shap
